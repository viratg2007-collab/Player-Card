# Copy to .env.local and fill with your Supabase project values
# (Project Settings -> API). The anon key is safe for client use; access is
# governed by Row Level Security. Leaving these unset keeps the app fully local
# and hides the cloud/Friends features.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
